{
  "name": "invoxl-monorepo",
  "version": "1.0.0",
  "private": true,
  "engines": {
    "node": ">=18.0.0"
  },
  "workspaces": [
    "backend",
    "frontend"
  ],
  "scripts": {
    "start": "npm start -w backend",
    "build": "npm run build -w frontend",
    "install:all": "npm install"
  }
}
